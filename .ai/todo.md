# Public mc replacement release

- [x] Write and review the release design and implementation plan.
- [x] Add the Apache-2.0 license and package metadata.
- [ ] Add the `mc` compatibility executable and verify argument errors use exit code 2.
- [ ] Add recursive `cp`, `mv`, and `rm` with unit and live tests.
- [ ] Add `mirror` with copy/update behavior and optional removal of extra targets.
- [ ] Add Linux amd64/arm64 OCI packaging and release automation.
- [x] Start MinIO locally and make every container smoke test perform real S3 operations.
- [ ] Add a live compatibility test matrix and comparison fixtures.
- [ ] Update the README and compatibility table.
- [ ] Run formatting, lint, local tests, container smoke tests, and live tests where credentials exist.
- [ ] Search repository issues and discussions, then record the results.

## Review

Pending implementation and verification.
